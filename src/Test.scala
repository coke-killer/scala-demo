/**
 * @description:
 * @author: yudongyue
 * @date: Created in 2021/4/9 15:25
 */
object Test {
  def main(args: Array[String]): Unit = {
    var a = 10;
    var b = 20;
    var c = 30;
    var d = 40;
    println("a+b=" + (a + b))
    println("a-b=" + (a - b))
    println("a*b=" + (a * b))
    println("a/b=" + (a / b))
    println("a%b=" + (a % b))
    println("b%a=" + (b % a))
    println("a==b=" + (a == b))
    println("a>b=" + (a > b))
    println("a<b=" + (a < b))
    println("a<=b=" + (a <= b))
    println("a>=b=" + (a >= b))
  }

}
